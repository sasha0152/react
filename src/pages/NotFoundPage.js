import React from "react";
import { Link } from "@mui/material";

const NotFoundPage = () => {
    return (
        <div>
          NotFoundPage <Link to={'/'}>Go to home</Link>
        </div>
    )
};
export default NotFoundPage;